{
  "name": "fandipres-site-build",
  "private": true,
  "version": "1.0.0",
  "description": "Build tooling for fandipres.my.id - prerenders JS-driven content into static HTML for SEO.",
  "scripts": {
    "build": "node build/prerender.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
